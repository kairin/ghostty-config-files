# Gemini CLI - Artifact Definition
# Format: type:path
# Types: binary, desktop, man, icon, config, dir
#
# Note: Gemini is installed via npm (fnm managed)
# The binary path is dynamic based on the active Node.js version

# Configuration directory
dir:$HOME/.gemini

# npm package location (fnm)
# Note: The actual binary path varies with node version
# Use `command -v gemini` to resolve at runtime
