# Logs Directory - Git Ignore Rules
#
# Purpose: Keep directory structure in Git but exclude large/generated files
#
# Last Modified: 2025-11-23

# Exclude terminal recordings (large files)
video/*.gif
video/*.mp4
video/*.webm
video/*.log
video/*.cast

# Exclude installation logs (generated on each run)
installation/*.log
installation/*.json
installation/*.md

# Exclude component logs (generated on each run)
components/*.log
components/*.json

# Keep directory structure
!.gitkeep
!video/.gitkeep
!installation/.gitkeep
!components/.gitkeep
