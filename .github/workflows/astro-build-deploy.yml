name: Constitutional Astro Build and Deploy (DISABLED - LOCAL CI/CD ONLY)

# CONSTITUTIONAL REQUIREMENT: Zero GitHub Actions consumption
# This workflow is DISABLED to enforce local CI/CD execution
# Use: ./local-infra/runners/gh-workflow-local.sh all
on:
  workflow_dispatch:
    inputs:
      force_enable:
        description: 'Emergency override (violates constitutional requirements)'
        required: true
        default: 'never'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.force_enable == 'constitutional-override-emergency' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Constitutional TypeScript Check
        run: npm run check

      - name: Build Astro site
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
