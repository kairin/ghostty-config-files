# Python
.venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# lib/ - Python build artifacts (DO NOT IGNORE root lib/ - our shell library)
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Website directory (new structure)
website/node_modules/
website/dist/
website/.astro/

# Astro
.astro/
dist/
.env
.env.production

# TypeScript
*.tsbuildinfo

# Logs (dual-mode output system)
# Ignore all log files in logs/ and subdirectories
logs/**/*.log
logs/**/*.json
logs/errors.log
# Keep directory structure
!logs/.gitkeep
!logs/installation/.gitkeep
!logs/components/.gitkeep
# Global log pattern
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# IDEs and editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Modern web development specific
*.tgz
*.tar.gz

# Local CI/CD logs (consolidated in .runners-local)
.runners-local/logs/**/*.log
.runners-local/logs/**/*.json
!.runners-local/logs/.gitkeep

# Performance monitoring
*.perf

# Temporary files
tmp/
temp/
*.tmp
# Root folder cleanup artifacts (temporary analysis files)
CLEANUP_EXECUTION_SCRIPT.sh
ROOT_CLEANUP_PLAN.md

# Generated analysis reports (should be in documentation/ or logs/)
*_ANALYSIS_REPORT.md
*_REPORT.md
FUNCTION_ANALYSIS*.md
DEAD_CODE*.md

# Installation state (runtime data)
.installation-state.json

# Build artifacts
build/
out/

# Documentation build output (generated by Astro)
# Per FR-007: docs-dist/ is gitignored to prevent accidental commit of build artifacts
# Only website/src/ should be committed (editable source files)
docs-dist/

# Cache
.cache/
.parcel-cache/
.next/
.nuxt/
.vuepress/dist

# ESLint cache
.eslintcache

# Stylelint cache
.stylelintcache

# Prettier cache
.prettiercache

# Package manager lock files (keep package-lock.json for reproducible builds)
yarn.lock
pnpm-lock.yaml

# Environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Deployment
deploy/
deployment/

# Constitutional compliance: Local CI/CD working files
.runners-local/tmp/
.runners-local/cache/
.runners-local/self-hosted/config/

# User customizations (preserve during updates)
configs/custom/
user-config/
# Backup files (added by migration)
backups/
*.backup-*
# TUI system backup files (lib/ directory backups)
lib/**/*.backup

# Lighthouse performance reports
documentations/performance/lighthouse-reports/*.json

# Package Migration System (005-apt-snap-migration)
# User configuration and runtime data
.config/package-migration/
~/.config/package-migration/
# Migration backups (excluded from version control per constitution)
~/.config/package-migration/backups/
# Audit cache
~/.config/package-migration/audit-cache.json
# Migration state
~/.config/package-migration/migration-state.json
# Temporary migration logs
/tmp/ghostty-start-logs/migration-*.log
/tmp/ghostty-start-logs/migration-*.log.json
.mcp.json

# Self-hosted runner credentials (CRITICAL - never commit)
# Security hardening per Context7 best practices (2025-11-12)
.local-runner/
~/.local-runner/
**/.credentials
**/.credentials_rsaparams
**/.runner
**/runner-*.token
runner-state.json
runner-credentials.json

# Runner service metadata (machine-specific) - moved to .runners-local/
# .runners-local/ directory removed - consolidated into .runners-local/

# GitHub Actions runner working directory
actions-runner/
_diag/
_work/

# TUI build artifacts (Go binary)
tui/ghostty-installer

# Download logs (wget artifacts)
wget-log
wget-log.*
