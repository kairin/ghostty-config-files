#!/bin/bash
# MCP Server Secrets Template
# ==========================
# Copy this file to ~/.mcp-secrets and fill in your API keys
# This file should be synced securely between machines
#
# Usage:
#   cp configs/mcp/.mcp-secrets.template ~/.mcp-secrets
#   # Edit ~/.mcp-secrets with your actual keys
#   echo '[ -f ~/.mcp-secrets ] && source ~/.mcp-secrets' >> ~/.zshrc
#   source ~/.zshrc
#
# SECURITY: Never commit ~/.mcp-secrets to git!

# =============================================================================
# Context7 API Key
# =============================================================================
# Get your key from: https://context7.com
# Used for: Up-to-date library documentation in Claude Code
export CONTEXT7_API_KEY=""

# =============================================================================
# HuggingFace Token
# =============================================================================
# Get your token from: https://huggingface.co/settings/tokens
# Used for: HuggingFace model hub access
export HUGGINGFACE_TOKEN=""
export HF_TOKEN="$HUGGINGFACE_TOKEN"

# =============================================================================
# GitHub Token (OPTIONAL - dynamic retrieval recommended)
# =============================================================================
# GitHub token is retrieved dynamically via 'gh auth token'
# Only set this as a backup if gh CLI is not available
# export GITHUB_PERSONAL_ACCESS_TOKEN=""

# =============================================================================
# Verification
# =============================================================================
# After sourcing, verify with:
#   echo "Context7: $(echo $CONTEXT7_API_KEY | head -c 10)..."
#   echo "HuggingFace: $(echo $HUGGINGFACE_TOKEN | head -c 10)..."
