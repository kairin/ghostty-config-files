# Scrollback (effectively unlimited with very large limit)
# Ghostty doesn't support -1 for unlimited, using 999999999 for practical unlimited
# This is ~1 billion lines of scrollback (effectively unlimited for practical use)
scrollback-limit = 999999999
